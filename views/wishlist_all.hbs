{{> header}}
{{> nav}}
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Wishlist of all users</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <table class="product_detail_table table table-striped table-hover">
                <thead>
                    <tr>
                        <th>S.no</th>
                        <th>Product Name</th>
                        <th>Sku</th>
                        <th>Username</th>
                        <th>Added on</th>
                        <th>Date</th>
                    </tr>      
                </thead>
                
                <tbody>
                    {{#each products as |productvalue productid|}}
                    {{!-- {{productvalue}} --}}
                    <tr>
                        <td>{{productvalue.serial}}</td>
                        <td>{{productvalue.productid.producttitle}}
                            <a href="/wishlist/edit/{{productvalue._id}}">Edit</a> | 
                            <a href="/wishlist/delete/{{productvalue._id}}">Delete</a>
                        </td>
                         <td>
                            {{!-- {{#each productvalue.deliverydate}}
                                {{this.categorytitle}}
                            {{/each}} --}}
                            {{productvalue.productid.sku}}
                        </td>
                        <td>{{productvalue.userid.username}}</td>
                        <td>{{productvalue.createdon}}</td>
                    </tr>
                    {{/each}}    
                </tbody>
                {{!-- <tfoot>
                    <tr>
                        <th>S.no</th>
                        <th>Name</th>
                        <th>Category</th>
                        <th>Sku</th>
                        <th>Type</th>
                        <th>Date</th>
                    </tr>
                </tfoot> --}}
            </table>
        </div>
    </div>
</div>
<!-- /#page-wrapper -->
{{> footer}}